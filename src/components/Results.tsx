import { View } from 'react-native';

const Results = () => {
  return (
    <View
      style={{
        width: '100%',
        flex: 1,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}></View>
  );
};

export default Results;
